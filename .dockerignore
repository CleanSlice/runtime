.git
.gitignore
node_modules
dist

# Agent runtime data (secrets, sessions — must not leak into image)
.agent/data
.agent/memory
.agent/sessions

# Environment and config
.env
.env.*
agents.json

# Build artifacts and caches
*.bak*
.cache
.tmp
