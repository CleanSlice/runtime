# HEARTBEAT.md

_Add periodic tasks below. Your assistant checks this file at the configured interval (default: every 30 minutes)._

_While no tasks are listed, checks are skipped entirely — no background LLM calls, no cost. Add a task line to enable them; it takes effect at the next check, no restart needed._

## Tasks

(empty — add reminders or periodic checks here)
