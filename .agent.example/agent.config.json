{
  "maxIterations": 25,
  "taskLabelLength": 60,
  "maxTokens": 16384,

  "_comment_access": "Access strategy: open | public | allowlist | code | approval (default). Runtime override via /access or set_access_strategy is persisted to data/access.json and overrides this.",
  "accessStrategy": "approval",
  "_comment_allowlist": "For allowlist strategy: list of allowed Telegram user IDs",
  "allowlist": [],
  "_comment_accessCode": "For code strategy: secret code users must send to get access",
  "accessCode": "",


  "heartbeat": {
    "intervalMin": 30
  },

  "session": {
    "compactionThreshold": 60,
    "recentKeep": 20
  },

  "_comment_memory": "limits: per-file character caps — oldest content is truncated from the top when exceeded. review: background self-improvement pass — every everyTurns turns, durable facts from the conversation are promoted into MEMORY.md via the auxiliary LLM. Set review.enabled false or everyTurns 0 to disable.",
  "memory": {
    "limits": {
      "soul": 6000,
      "user": 1375,
      "memory": 2200,
      "heartbeat": 1000,
      "daily": 4000
    },
    "review": {
      "enabled": true,
      "everyTurns": 10
    }
  },

  "_comment_s3": "Sync is event-driven (fs.watch + diff). syncIntervalSec is an optional periodic full-sweep safety net — set 0 to disable, or e.g. 21600 (6h) for paranoid backup.",
  "s3": {
    "syncIntervalSec": 0,
    "watcherDebounceMs": 30000
  },

  "stopPhrases": [
    "/stop",
    "stop", "abort", "cancel",
    "halt", "enough", "quit"
  ],

  "_comment_managed": "Files overwritten from .agent.example on every restart. Set to [] to disable.",
  "managedFiles": ["SOUL.md"],

  "_comment_syncSkills": "Sync skills from .agent.example on restart. User-created skills are never deleted.",
  "syncSkills": true,

  "tools": {
    "spawnAgent": {
      "timeoutMin": 5,
      "outputLimit": 4000
    },
    "browser": {
      "outputLimit": 5000
    },
    "webFetch": {
      "maxChars": 8000
    }
  }
}
